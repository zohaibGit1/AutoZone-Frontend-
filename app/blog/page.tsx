import Image from 'next/image'; import Link from 'next/link'; import { ContentShell } from '@/components/content-shell'; import { articles } from '@/lib/data'
export default function Blog(){return <ContentShell title="Blog" eyebrow="Our News" image="/images/blog-polish.png"><section className="container grid gap-px bg-white/10 pb-28 md:grid-cols-2 lg:grid-cols-3">{articles.map(a=><article key={a.slug} className="bg-[#111015]"><div className="relative aspect-[4/3]"><Image src={a.image} alt={a.title} fill className="object-cover"/></div><div className="p-7"><p className="font-heading text-xs font-bold uppercase text-[#ea0a0b]">{a.category} · {a.date}</p><h2 className="mt-3 text-3xl">{a.title}</h2><p className="mt-4 text-sm text-[#8b8b90]">{a.excerpt}</p><Link href={`/blog/${a.slug}`} className="mt-6 inline-block font-heading text-sm font-bold uppercase text-white hover:text-[#ea0a0b]">Read More</Link></div></article>)}</section></ContentShell>}
