import Image from 'next/image'; import Link from 'next/link'; import { notFound } from 'next/navigation'; import { ContentShell } from '@/components/content-shell'; import { articles } from '@/lib/data'
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export default async function Post({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)notFound();return <ContentShell title={a.title} eyebrow={`${a.category} · ${a.date}`} image={a.image}><article className="container max-w-4xl pb-28"><div className="relative aspect-video"><Image src={a.image} alt={a.title} fill className="object-cover"/></div><p className="mt-10 text-xl leading-relaxed text-[#b9b9bd]">{a.excerpt}</p><div className="mt-8 space-y-6 text-[#9a9aa0]"><p>Professional detailing is a combination of preparation, controlled products and careful finishing. The right process depends on the condition of the vehicle and the protection the owner wants.</p><p>Before any service, inspect the paint, wheels and interior. This helps the technician choose the correct process rather than applying the same package to every car.</p><p>For ongoing results, regular washing with suitable products and periodic maintenance are just as important as the original detail.</p></div><Link href="/blog" className="mt-10 inline-block font-heading font-bold uppercase text-[#ea0a0b]">← Back To Blog</Link></article></ContentShell>}
